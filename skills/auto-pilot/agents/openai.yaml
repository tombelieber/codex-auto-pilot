interface:
  display_name: "Auto Pilot"
  short_description: "Dynamically execute an approved spec into a verified PR"
  default_prompt: "Use $auto-pilot to dynamically execute my complete approved spec and deliver a production-ready PR."
policy:
  allow_implicit_invocation: false
