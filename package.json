{
  "name": "codex-auto-pilot",
  "version": "0.2.0",
  "description": "Safe installer for the minimal Codex Auto Pilot delivery skill.",
  "bin": {
    "codex-auto-pilot": "bin/codex-auto-pilot.mjs"
  },
  "scripts": {
    "check": "node --check bin/*.mjs && node --check lib/*.mjs && node --check scripts/*.mjs && node --check test/*.mjs && node --test && node scripts/check-public-safety.mjs && npm run pack:dry",
    "test": "node --test",
    "pack:dry": "npm pack --dry-run --json"
  },
  "files": [
    "bin/",
    ".codex-plugin/",
    "lib/",
    "skills/",
    "install.sh",
    "README.md",
    "LICENSE"
  ],
  "keywords": ["codex", "automation", "delivery", "skills"],
  "homepage": "https://github.com/tombelieber/codex-auto-pilot#readme",
  "bugs": {"url": "https://github.com/tombelieber/codex-auto-pilot/issues"},
  "repository": {"type": "git", "url": "git+https://github.com/tombelieber/codex-auto-pilot.git"},
  "author": "tombelieber",
  "license": "MIT",
  "engines": {"node": ">=18"}
}
